# Course 3: Setting Up Statsig — Combined Lesson Captions

_Combined caption text from all 12 lessons in this course. Original individual caption files remain at `data/raw/captions/` for granular citation use by the embedded skills (statsig-ask, statsig-quiz, etc.). This combined file exists primarily for NotebookLM ingestion under its 50-source cap._

---

## Statsig 3.2 - Install and initialize the Statsig client SDK

_Source: `data/raw/captions/Statsig 3.2 - Install and initialize the Statsig client SDK.txt`_

Hi. I'm Tyler Van Herron. I'm an engineer here at Statsig. And today, we're gonna be talking about using Statsig SDKs on the client side. In particular, we're gonna be making use of the Statsig React SDK today. And we'll sort of be going from having a super simple React app to actually integrating with the Statsig SDK, using it for things like having a gate, being able to log events, etcetera, and really taking you through that process. So to start us off here, I have a really simple React app. Really just here is our route. It has one request to to a node back end that it'll return, and then also has another component that is like some incrementer just so that we have something a little interactive. Right? And so we're gonna take this, and we're gonna start instrumenting it with stat sig. That'll let us do a number of interesting things. The first is sort of just control which features are available to folks using feature gates, start getting into how we could then do, like, deeper targeting or experiments on top of those, as well as talk about just, like, getting sort of basic instrumentation set up for my app here. The first thing you're going to need is, obviously, your app, but then also an account on stat sig. That will give you access to the SDK keys for your project, as well as let you start getting, you know, environments set up. The other thing you will need to do as part of this is just install the Statsig SDK. That's really just gonna be an NPM install like this. I've already run this particular command here, but just, you know, install it for your project. Once you've gone ahead and installed the stat sig react bindings, now we're actually good to go and start making use of it. In particular, the way the Statsig sort of React SDK works is we enclose your app in what we call a Statsig provider. You want this as close to the root of your app as possible. Really, it's just any children components of the provider are where you're going to be able to make use of stat sig. So generally speaking, as close to the root as possible is is sort of best there. So now that I have gone ahead and installed the React bindings, I can actually go ahead and do something like this where I go to my app, which, again, here at the root. I'm gonna go ahead and import the stat provider, and I'm just gonna wrap this with the stat provider in lowercase. There we go. There we go. The other thing that you're gonna need to put into the stat c provider is which, like, SDK key we're initializing with. You can find this by going in your project. And if you command k, you can actually search for things and get SDK keys keys. You can also find this by clicking on project settings and going into your keys and environments. Here, you'll see your sort of start off with a client SDK key that you can make use of here. In particular, for my project, it is this, but you should, of course, use your own. And you'll just pass that into the provider. That means that on your requests, we know it's for your project. Right? Now I'm sort of set to start using Statsig in its most basic form. The way I'll go ahead and do that is by doing something like this, where I'm going to make use of our hooks. It makes it really easy to access the static provider no matter where you are in your tree structure. So in one of my sections, I'm gonna do something like this where I just call the static client and, of course, import it like that. And now I have this client. And on that client, I can call Checkgate. So in this case, we're gonna go ahead and make a gate called something like show counter just to sort of control if this counter element is there or not. Once you've gone ahead and created your gate, it's really straightforward to go ahead and make use of that. You're just gonna take your client and call CheckGate on it. So for this one where I'm, like, saying I have gate that's going to see if someone should have access to this show counter. It'll just look something like this. In particular, you wanna copy this gate ID and make use of that. It'll just be sort of lowercase, underscored, etcetera. And now I have this counter behind the gate. Sort of by default, this gate will return false. I can sort of edit this gate to be that now. And if I go ahead and restart my app just to make sure it picks up that change that I added, the Statsy SDK, You'll see that, again, if I go to this app, yeah, it no longer has the counter there. And now I can go ahead into show counter and add a rule and say, hey, everyone should have access to this feature. A hundred percent of people should have my counter. Go ahead and save that. And then in a few seconds, Statsig will sort of pick up that change. And now the next time someone comes to this page, the counter's back. Again, now I just have this sort of nice on off switch that I can make use of, and that will sort of allow us to control who sees or doesn't see my counter. I can even check out diagnostics and see when folks are coming through and actually getting evaluated for this. So here, I can see a call to show counter, what user object I evaluate with evaluated with. In this case, it was empty. But with an every one hundred percent zero percent rule, that'll sort of allow you to just have that on off switch. The next place where this starts getting interesting is if I wanna start doing things like giving this to only certain people. So with that, we have the Static SDK initialized. I can make use of it for these pretty simple on off switches. The next topic we're gonna cover is actually going and providing more information about the user so that I can do more advanced targeting than just those pure on off switches. But, yeah, check out our next video for more on that topic. Thanks.

---

## Statsig 3.2 - Install and initialize the Statsig server SDK

_Source: `data/raw/captions/Statsig 3.2 - Install and initialize the Statsig server SDK.txt`_

Hey. I'm Tyler Van Herren, an engineer here at Statsig. And today, we're gonna be talking about getting started with initializing your SDK for server SDKs. In particular, in this one, I'm going to be using Node as my sort of example, but concepts apply to broadly all of our server SDKs. And so I've got a really simple React app here that just makes a request to basically the most straightforward back end you can have. And we're gonna start making use of Statsig in this back end just to modify sort of what message we're sending back. So the first thing that you wanna do as part of making use of Statsig is actually go ahead and get it installed. In particular, for me, I just did an NPM install for the Statsig node package. Use sort of the corresponding guide there, of course. And once you actually have that installed, you can go ahead and initialize the SDK. And so once I've gone ahead and installed the Statsig SDK, I can go ahead and start making use of it in my my app. In particular, I'm obviously going to want to sort of, you know, get it imported. But then I can also go ahead and start initializing. So that will look something like this. Just calling this await initialize. So await stats dot initialize. Generally speaking, this is something you're gonna wanna do on sort of skirt server spin up as part of that process. Here, I'm sort of initializing on request. The other thing that you'll want to do sort of correctly is actually do proper secret management. For this particular demo purposes, I'm just going to take my key and paste it in directly. Of course, do not do this with your actual secrets. Do your proper secret management services. I'll just deactivate this one after after this demo. But I'm going to go ahead and initialize the SATSYK SDK and say, hey. This is my SDK key. That SDK key is really important, really valuable because it lets us know what configs to actually go ahead and give you. While our client SDKs, it's one user at a time, so we can actually do a lot of storing, hey, this user has these values for these gates. For your server SDKs, like, we're expecting you to have many, many different users making requests, etcetera. It's not tied to one user. To handle that, we go ahead and actually download all of the rules you need to evaluate locally to your servers. Servers. So that's kind of like the secret sauce of how all of your evaluations work, and that's why you wanna be sort of protective of it. We're gonna go ahead and initialize with that. And now that And now that I'm initialized, further initializes are actually just gonna be sort of no ops. So it's not the worst case scenario, but, like, adds a weird latency to this request. So do it do it correctly. Do it on server start up. The other thing that we're need in order to actually go ahead and check a gate, and I've gone ahead and made a gate here that we'll use called show new back end message. And we're going to say, who is our user? This is something that will usually be coming from your requests or your, you know, maybe adding additional authenticated information to the the object, the request. And you're gonna be making use of that to know, like, what sort of targeting can you do, who is this person. If I'm rolling something out partially, what does that look like? Things like that. In this case, I'm, again, going to do something super simplistic here and just say, like, yeah, my user ID is one two three four, something like that. And now I have a user that I'm going to use for answering this. Now that I have a user, now that I have a gate, I can go ahead and actually use the gate. So do something like it's actually pretty pretty good. Should I use a new message? And that will just check the gate, whatever you called it, show new back end message, and then pass in that user object. In that user object, I could also put all sorts of other information. If I have an email that I wanna be able to target off of, I could add the email included in it. If I have other things that I am super interested in, like what country the user is in or things like that, I can make use of that here as well. There's also a field called custom that will allow you to add sort of anything you wanna target off of. Common example here is something like subscriber. Is this a person who should have access to special features that maybe normal people shouldn't have? You could put that in here too and say, yeah, subscriber. Maybe this user is not a subscriber. And now I could roll out things based on that status too. For a lot of this, this is gonna be stuff that you're fetching from your other servers, memoizing, etcetera, and having on your user object. And so now that I have my user object, now that I have the actual SDK initialized, I can go ahead and make use of check gate and say, like, yep. I'm checking gate for this user, and I'm checking this gate. I can check any of my gates here now and actually just get back that Boolean. Do they pass? Do they fail that gate? Should they have or not have that feature or that new version of the feature, etcetera. I can go ahead and make use of that show me message here. And in this case, if they fail, I'm going to return my current message of the messages coming from the back end. And if they pass, I'm going to return this new message. So now that you're checking your gate, one other thing that you might want to do, depending on sort of the setup of your server, is manually flush. For really long running servers, this is probably not a huge issue. If you're using Statsig concept of, like, a script or in the concept of potentially even like a Lambda, it's useful to call flush at the end. By default, we'll be flushing every ten seconds. We'll try to flush on shutdown as well just to get all of those events of who saw what gates, who saw who checked what gates, etcetera, sent over to you. But if you're in a, like, sort of very short living environment that ends very abruptly, you'll potentially want to do something like call flush here as well just to make sure the events get our way. Now when I go and actually check my app, I'm, in this case, getting back that message coming from the back end on account of it not letting anyone through. If I go over to diagnostics, I should actually be able to see things coming through as well. Yeah. True. It does have no rules. I'll be able to see yep. That was an earlier evaluation. But now I'll be able to see that false come through. Someone checked this. They got back false. The reason they got back false was because there's no rules, so it will return false by default. I can go ahead and add rules on top of it. So in this case, I'm gonna go ahead and let everyone through. Every single user is allowed into this gate. And now when I go back over to my application, in about ten seconds or so, I should see this kick in. It will take low single digit seconds for stat sake to propagate out this information to sort of our global knowledge. And then your server is likely pulling every ten seconds for updates. So, again, in about ten seconds, I see that now I passed that gate. I get the new message from the back end. If I go over to diagnostics, I should see a new exposure. Honestly, probably is very similar to this one because I had that rule in there earlier where I see, yep, for user one two three four, they checked this gate. They got back true. The reason they got back true is because they passed that everyone rule. If I did something like set this gate to fifty percent of users, now fifty percent of user IDs would start passing instead of a hundred percent or zero percent. That's sort of the basics of getting up and running with stat sig on the server side. Yeah. Stay tuned for more videos and enjoy stat sigging. Thanks.

---

## Statsig 3.2 - Introduction to client and server SDKs

_Source: `data/raw/captions/Statsig 3.2 - Introduction to client and server SDKs.txt`_

Hi, I'm Tally Van Herren. I'm an engineer here at Statsig. And today we're gonna be talking about the SDKs. Broadly, our SDKs are ways for you to sort of connect your application to Statsig. And that can come in a lot of different forms. Some of that is going to be when you're using our SDKs for bucketing or assignment. In particular when you're making use of feature gates or experiments. That's gonna be how you actually know should this person get this feature or should they not. Or in the case of an experiment, which group should they be in. There are also a really convenient way for you to be logging stuff back to STAT6 so that you can do readouts based on that. There's two forms of SDKs that STAT6 has, our client SDKs and our server SDKs. Obviously our client SDKs are gonna be used more in those client side locations, right? So iOS code, Android code, JavaScript. And then on the server side, that's of course gonna be when we're running in your servers. There's a few different design considerations that go into that from the SATSIG side. We'll talk a little bit about when they make sense to use on your end, what types of things make sense to do there, and how they work. So the client SDKs are obviously when they're running on an end user's device. So again, that will most likely be in their app or on their browser. And so for those applications, this is when we're operating in the context of a single end user. Obviously only one person is using the app at a time on one device. And so we get to do a lot of things, again only in the context of that. In particular, when you initialize the Statsig SDK, it's gonna go ahead and just hand over to Statsig, hey, this is who I am. This is everything there is to know about me that is pertinent for Gates. And then Statsig will respond with all of the evaluations that they might need during their session. What that's going to do is mean that when you want to do subsequent checks post initialize, there's actually no network call that's required in order to do those checks. That keeps things really snappy, really performant on your end user applications. We will, in the background, log things back to us saying, hey, this check happened. That's gonna be really important for the analysis piece. But ultimately there's no blocking network calls happening there at all evaluations once the client SDK is initialized. The thing that's worth considering here in terms of when this makes sense to do is that anything that is happening on an end user's device is to some extent a little bit inherently insecure, right? They have quite a bit of ability to poke around, peek around on any code that's obviously running on their devices. And so while there are a number of things that Statsig does to help obfuscate some of this and help keep things a little more sort of kept out of prying eyes, you should probably not be doing security critical type things as client side experiments. Rather, we'd recommend those types of things be happening in your servers, on your servers. For our server SDKs, the architecture is a little bit different. As you might imagine, one server, one pod of yours might be processing many, many end users as requests come in from all around the world. And so rather than trying to have Statsig give a response for an individual user from our servers to yours, what we do instead is we actually download rule sets to your server that are going to allow you locally to evaluate a given gate or a given experiment for a given user. That means that again, there is actually no network call that's required for an individual check gate or get experiment call even though you have the ability to do this for any of your end users. This, again, keeps things local, keeps things really performant from that perspective, and then we will do those sort of polling back to us. Again, this is going to be most useful for those applications, for those cases where you want to do things that are sort of again kept away from your end users or sort of otherwise isolated from that. In addition to all of the really cool experiments that you can actually do like infrastructurally. We've seen tons of people do really interesting experiments where they change their caching strategies, change how they compress something, things like that, where obviously it makes sense to do server side. But then again, you can run most experiments from the server side and then it's a matter of sort of giving to your clients the results of them rather than exposing the experiment itself as happening. But yeah, between our server and client SDKs, you should be able to use Statsig wherever you're already making changes, already making product decisions, or again, infra decisions. And be able to tie all that together to all the really awesome metrics that you might care about. Thanks. Something like that? I guess I can stop.

---

## Statsig 3.2 - Optional advanced setup

_Source: `data/raw/captions/Statsig 3.2 - Optional advanced setup.txt`_

Hi. I'm Tyler Van Herren. I'm an engineer here at Statsink. And today we'll be talking about some of the more advanced options or configurations you might be running across when you're using our SDKs. These sort of broadly allow you to solve for a few different interesting use cases and really give you a ton of peace of mind about what your failure states look like and things like that in addition to some nice performance wins. The first one I wanna talk about is bootstrapping. What this is is using one of your servers running our server SDK to actually prepare the sort of initialization response that one of our client SDKs would require. Essentially what this means is that rather than having your client SDK reach out to StatSig servers and get a response from them, you're able to have your servers prepare a response and hand it directly to that client SDK for that given user. From a performance perspective, this gives you some nice benefits where generally speaking, if you're using us in addition to some network request that's already happening to your servers, you have some request that happens on app launch, app startup, that'll go to your servers and then back to prepare that initial user. And then after that, there's a separate hop from that app or from that website over to Statsig and back. And so we can actually largely remove one of those hops by having that initial ping to your servers bring back all the stuff that the Statsig SDK needs to get started. The other sort of big benefit here is just from a failure mode's perspective. While our client SDKs will fall back to cache if they have trouble sort of reaching out to the mothership of Statsig, this allows you to have the actual sort of root initialize be controlled by your infrastructure, which means that if in the unfortunate event stat sig is having issues, you'll be pretty isolated from those on the client side. Data adapters, on the other hand, are sort of the server version of some of that fallback from some of that safety. What a data adapter is going to do is allow you to store in your fleets, in your sort of What a data adapter is going to allow you to do is sort of in your fleets store a copy of those rule sets in like a Redis or something like that that will allow you to initialize your server SDKs from that locally held copy rather than having them ping our CDNs for that copy. And so what that's going to allow you to do is, again, give you a really nice isolation from STAT where even if for some reason you're not able to establish that connection, you will have that local copy and allow yourself spin up net new servers using the Statsig SDK from that saved version of the rule sets. In addition, our server SDKs will also handle updating those rule sets so that when things are good, they will keep that sort of saved copy up to date. What that looks like in practice is really just providing an interface on top of some storage that our SDKs know how to talk to. So we have a class that you can implement, and then from that point on, we're able to have the methods that we expect in order to write and read from those storage locations. This is also really nice, again from a performance perspective, for things like Lambdas or Edge Workers where you might, for these short lived servers, actually find that that initial initialized hop is a relatively large percentage of their lifetime. And this allows them to sort of just do that from in cluster. Sort of the related thing to the data adapter is gonna be the forward proxy. The forward proxy is sort of like the ultimate of putting Statsig's infrastructure in your infrastructure. What this is is a dedicated image that you can host that will handle all of the communicating to Statsig from your side. Ordinarily, of course, our server SDKs are sort of all pinging back to Statsig, pinging for updates on the rule sets themselves. And at scale, that can actually wind up being a lot of network chatter. And so what this allows you to do is centralize that talking to statsig into one or a smaller number of pods that are going to be fully responsible for that. And what this also enables is a lot of really cool in clustered communication where you're able to do things like gRPC streaming from those servers to your servers that are, again, running in that infrastructure. So that is really helpful for that sort of thing. And again, they play really nicely, of course, with the data adapter where they can keep that up to date for you. The last two topics that I'll talk on are a little bit different. One of the most common topics that we'll hear about is how to think about PII. Our general guidance is that you should not be sending PII to Statsig. We don't want that in your analytics logs that are coming to us. We want that kept away from us. But there are sometimes cases where you find that those are still types of things that you would want to be targeting on, even though you wouldn't want them to be showing up in your logging. For those types of cases, we have a field known as private attributes where it's sort of designed for those types of custom attributes, but things that should be sort of kept away from stat sig. For our server SDKs in particular, those never leave your servers. They're stripped away before any logging makes it back to stat sig, while still allowing that local evaluation to take advantage of them for targeting. For client SDKs where you're not bootstrapping, they will be in that initial payload back to stat sig, and then we will strip them away after sort of that initial evaluation that we get back to you. But again, with the server SDKs, they never leave your infrastructure, which is pretty awesome. Of course, when you're making use of those, you should remember that you're making use of those when you're looking at why a user got the experience they did because you might find that when you're looking at the logs in stat sig, they don't have some attribute that would explain that. It might be because it was a private attribute. The final thing I'll hit on here is just the concept of environment tiers. Broadly, we are very much big proponents of this, big recommenders of this, that you should actually have different client keys, different server keys for your different tiers, whether that's your UAT tier, your intern tier, your dev tier, your staging tier, and then of course your production tier. And what that's going to allow you to do is across all of your gates, across all your experiments, you'll be able to roll things out to those lower environments independent of what's happening to the production environments. And there's even some nice settings that are present around our review flows that allow you to give people flexibility in like the dev tier where they're able to make changes, able to iterate quickly without needing to go through the entire review process, but you can still have those safeguards in place for things that'll hit your end users. So we'll definitely recommend that you mirror the tiers you have in Statsink to the way you think about deployments sort of broadly. And we'll also recommend splitting out keys for those as well as, of course, for those different applications or different services that you might have. Thanks. Well, that was

---

## Statsig 3.2 - SDK logging events and exposures

_Source: `data/raw/captions/Statsig 3.2 - SDK logging events and exposures.txt`_

Hi. I'm Tyler, an engineer here at Statsig. And today, I'm gonna be talking a bit about what's sort of happening under the hood as you're making use of Statsig and sort of helping you understand what's actually happening here, particularly what sort of information is coming back to us and how does that, sort of used. And so in our last couple of videos, we talked about getting a super simple React app up and running, making use of SATSIG in that React app, in particular for doing things like checking a gate. But let's talk a little bit about how some of that is getting sent back to us. In particular, one concept that you'll hear a lot about when talking to Statsig or about Statsig is what we call exposure events. What that is is saying, hey, this person saw this experience at this time. When you're looking at diagnostics for a given gate or experiment, you'll see a sort of somewhat undedupped version of that. We are applying deduplication to that for things like the same user checking the same gate a bunch of times in a row. But generally speaking, you're seeing just exposure events flowing through. Flowing through. There's sort of two modes that you could think of for how experimentation works or, like, analyzing results works. One is you could have this, like, giant table of every single one of your users and say, hey, this user should get this gate or this experience on this gate. We don't actually have that giant table. In particular, it would be kind of onerous for us to transmit that giant table over to your servers and have you have to store that giant table too if you're using a server SDK. So rather that we do everything sort of very deterministically and then just write down, hey. This person saw this thing. So what's coming back here is those, hey, this person saw this experience that is going to get transmitted to us every time one of these check gates happens with some deduplication. The other type of information that you'll often send our way is what we call log events. So in addition to saying, hey, this person saw this version of this GATOR experiment, and that lets us, you know, sort of say, cool. Who what who saw test? Who saw control? Let's compare some metrics. You might also log a bunch of stuff to us that sort of forms the the foundation for those metrics. In particular, I have this sort of counter React app. I might wanna know, hey. Like, how many times are people pressing increment? How many times are people pressing reset? Right? So I could go ahead and actually make use of the same client that we used for checking gates and actually go ahead and log events with it. So on that counter itself, I'm gonna go ahead and import the client again. I'm going to grab the client again. And then when we increment, rather than just setting our count, I'm going to also do what we call logging an event. Counter or maybe sort of increment. And then here on the reset side, I'm going to do something very similar and say client dot log event reset. And so now every time someone goes and presses one of these buttons, it's going to tell SATSIG and sort of therefore you, hey, someone actually went and pressed this button. It will have all the information about them that you already have in the user object that we initialized with previously, and that lets you do a lot of sort of very straightforward metrics sort of out of the box. And so now I'm actually logging events back to Statsig. Those events will hit us and then take a little bit to process. But in about, you know, ten, fifteen minutes, you should start seeing, hey, this event happened for this user, and furthermore, being able to, like, start making metrics on top of that. And so with that, you kind of have the foundations to not only, like, have all of these switches that are controlling who is even seeing parts of your app, seeing different versions of things in your app, but actually, like, actually logging things to make metrics out of to understand, hey, you know, obviously, if I remove the counter, the group without the counter is not gonna increment very much. But if I do something like change the text for my increment button or change how much it increments by or something like that, is that more engaging for folks? Are they incrementing more? Things like that. You can actually start to understand real user behavior on your app. In the next episode, we'll talk about sort of the server side versions, a lot of these sort of client side features, and some of the differences between the two. Thanks.

---

## Statsig 3.2 - SDK user object and targeting

_Source: `data/raw/captions/Statsig 3.2 - SDK user object and targeting.txt`_

Hi. I'm Tyler Van Herren. I'm a software engineer here at Statsig. And today, we're gonna be talking about providing information about the user when you're initializing your SDK, particularly for client SDKs, in this case, the React. And so last video, video, we chatted about the sort of most basic way to initialize the SDK. We did not provide any user information. So all we could really do are these, like, pure on off switches. Right? In this episode, we're gonna be talking a little bit more about how to provide more context about the user, make use of that for targeting and things like that, and then sort of that transitions us into how you can actually go and run experiments. So one thing you maybe notice in our docs is that in our initial initialization, there's actually the option to provide this user object. This is where you can give us all of the context you have about the user. There's a few things that we will pick up sort of automatically for you. We can actually see some of those if we look at the diagnostic events that got fired last time. In particular, there's stable ID. You can think of that sort of as like a device identifier that our client SDKs will generate, keep in local storage, and just have as something for you to target off of. So I could actually roll a feature out to fifty percent of devices. But a lot of times, that's not quite what you're looking for if you have a fee a, you know, project where people sign in and they wanna actually be able to have the same account on different things, have different, you know, have the same experience across different devices. The other things that will sort of be auto generated by default are things like country, are things like operating system, things where, like, our client SDKs can sort of pick that up from the user op from the user agent itself. That's what allows us to do some of those, like, basic things for for free. So, in fact, I could actually go and add some rule here that would do something like, hey, I only want to release this feature in the US. You know, only US users should be allowed to use my little counter increment here. So there's all those features that will sort of auto infer, including sort of that, like, stable ID that will auto generate just to represent the device. The other thing that we can add in here is when we're initializing the SDK, we can go ahead and actually pass in a user object. What this is, again, is really just sort of a blob of information you want us to have about the user. So some of that is going to be things like, you know, a user ID is a really common one. So, again, if I have a signed in user, I might just sort of put the user ID that I have in there, whether that's their email or you generate some identifier for them. As long as you can sort of consistently provide that across different applications, now you can have people have the same experience on different devices. You can also override some of our top level fields. So if you have your own way to determine what country a user is associated with, you can absolutely do that override as well in that same spot. And then there's also this neat section called custom. And in custom, you can put sort of any other information you want about the user, whether that's something like, are they a subscriber? I can, you know, add that information here as well and say, like, yes, this is a subscriber. So maybe they get access to some subscriber only features. Other information that people find useful to put there tends to be things like account age, super useful for doing, like, new user targeting, things like that. Those are the types of things. But really, if there's something where you're like, I wish I could give this feature to only people who meet these criteria, you can kind of provide that criteria in custom and make use of it. The final thing I'll call out here in the user object is actually custom IDs. When you are doing actions on your site, you can think of yourself as, like, having multiple identifiers corresponding to yourself. A good example is when I am actually in Statsig itself. I am clicking around. I am on a device. I'm on my laptop right here in this Chrome browser. So that is sort of where my stable ID is coming from. I'm also logged in to Statsig as myself, as Tyler Van Herren. And then furthermore, I'm actually in a project. I'm in this Danmo project. And so when Statsig is thinking about releasing features, we actually get to, like, choose sort of which of those identifiers we're going to going to use to release features and also then to analyze on correspondingly. So if I'm running some logged out test, I'm probably gonna use stable ID and just, you know, your device will be your identifier. If I have some logged in feature, I want to have it be consistent across experiences, I'll go ahead and use user ID. Then when I log in, I will always have the same experience. And then a lot of times for stats sake, it makes a lot of sense for us to release features based on project ID or or, like, you know, sort of company so that when you look at your neighbor's screen, you're seeing the same things. So I could go ahead and add, you know, for instance, project ID. Project ID here and say, yeah, that's my project ID. Now I can actually run tests on project ID as well. So that's the sort of information you can put in the user object as well as what we'll sort of auto infer for client SDKs. Let's talk about how to make use of some of that. What it looks like is for those custom fields, there is a criteria here you can choose called custom field. And then you can actually choose, like, which of those keys you wanna make advantage of. So I could add some, like, subscriber and only actual subscribers get to pass, you know, maybe subscriber early access. And so I would have something like this where now those users would have access to my feature even if maybe it's not rolled out to everyone yet. So if I save that and I again, just because I'm sort of manually setting these values, I'm just gonna restart things here. Now because I am a subscriber, when I look at this, I will actually see that counter because, again, we actually just have that getting used here. We're just checking that gate, seeing if they should have access to the counter. And now if I were to initialize without being a subscriber, and then I think the hot refresh should yep. So now that I'm not a subscriber, I don't have access to that feature anymore because we set it up that way and checked it in code. And so the other thing that we can start to do now that we're passing in user ID and, you know, we also have access to the stable ID is we can start doing, like, percentage rollouts. Rather than using this as purely an on off switch, I actually get to do things like say, hey, fifty percent of users are going to get this feature, whether that's for, you know, maybe I just want to slowly release something, make sure I don't get bug reports, or maybe it's actually want to run some experiments and see that my metrics don't regress. And so you'll specify which ID type you're using for a given gator experiment. It will be sort of set in stone for that experiment or that gate. You can change it to a different one, but it has one ID type. You are releasing it to fifty percent of users, not fifty percent or fifty percent of devices, not some weird mixture of the two. And so with this gate and with me providing an ID, I can actually go ahead and say, like, yeah, actually, I will release this to fifty percent of users. We can actually go ahead over to diagnostics and also see sort of which users saw what experience and why from the Statsig side. This is super helpful for debugging what you were seeing with your users as well as understanding just like, you know, are the people I expect to pass passing, why are they passing, etcetera. I can go ahead and look at some of these and see that, yep, here's one two three six. On that everyone rule, was at fifty percent, they got true then and there. And then I can also see over here a different user, one two three seven, who failed on that same rule. That is sort of them passing, failing then and there, and getting those sort of responses back. Diagnostics also lets you do all sorts of filtering, super handy for tracking down why someone did or didn't get the experience that you expected them to. The other cool thing that you can do is you can actually copy any of these user objects, go ahead over to setup, and paste them into this test gate, and see sort of what you expect them to get. So I could similarly go through my user IDs and increment them. We can double check that, like, hey. If this was a six, yep. They passed. If this was a five, they also passed, but maybe eight. I don't know if they would pass or not and so on and so forth. As I add more, we'll find some also who fail just to sort of the fifty fifty. Although this one's getting pretty pretty lucky. There we go. And, yeah, that's sort of how you can start adding more information about your user to what you initialize with, making use of that in stats again, targeting off of that, rolling out features based on that, things like that. Yeah. In our next videos, we'll be talking more about what this looks like both from the server side as well as going sort of deeper on some of these. But, yeah, stay tuned for more. Thanks.

---

## Statsig 3.2 - Testing and diagnostics

_Source: `data/raw/captions/Statsig 3.2 - Testing and diagnostics.txt`_

Hi. I'm Tyler Van Herren. I'm an engineer here at Statsig. And today, we're gonna be talking about testing out your gates, etcetera. How to go about doing that, how to make sure you're ready to actually start hitting production with your sort of Statsig powered code. The sort of most versatile tool in your arsenal is going to be overrides. It's a way to force a given experience for a given ID. So in particular, this usually looks like I am building something myself. I know what my ID is. In this case, it is because I have hard coded it in. I am user one two three seven, but it's probably just that you know what your ID is. Maybe that is specific to your dev environment, staging environment, whatever. But you have some way to identify yourself. And so a really common thing that you'll want to do is make sure that you have like, try out the experience in both of those flows. Right? That's a it's a pretty reasonable request. And so right now, if I check this gate, you'll notice I don't get this counter because I'm that feature hasn't been released yet. If I look at this, we have not rolled it out to subscribers yet. We have not rolled it out to everyone yet. No one has access to this thing, but I want it for me. I'm gonna go ahead and add an override. In particular, I am one two three seven. Maybe this is only for when the app is running on, you know, again, staging or product development environments. In this case, I know that for my demo app, IDs are are you are globally applicable across environments. So I can go ahead and add them. And now when I go ahead and save that, it should be the case that, yep, now I have access to this feature. So this is a really simple way for particularly developers or, like, QA teams to make sure they're trying out different experiences when they're trying to try out something live. There's sort of different paths you can go down for, like, for automated testing type purposes. But for just being, like, give me what control is seeing, give me what test is seeing, give me this feature, give me not this feature, you can use this to, again, override yourself into or not into different environments, things like that. The same is also applicable to experiments for what it's worth. You can very similarly go to manage overrides for the experiment and add yourself to one of those arms experiment to test it out. The other thing that will come into play for understanding, like, how healthy your experiment is or your gait is tends to be starting in lower environments. When you have one of these rules, you can select which environments this is applicable for. So in particular, let me go ahead and add some, like, development only where this will only pass users on the development environment, and then move this up to the top. Oh, move it up to the top. There we go. So now when I look at this gate, everyone on staging tier, on production tier are not going to have access to this feature, but everyone on development is. This is typically like the sort of next step. Once you've validated that it works for you, you start letting the other people at your company start seeing this thing before actually real people will see it on real environments. That environment is going to get set when you are initializing your SDK. Usually, it's just gonna be coming from an environment variable or something like that. But you'll pass it into the static options that you can pass into the static provider that is sort of wrapping your whole app and include environment and then what tier environment it is. The sort of out of the box ones from our perspective are going to be development, production, and staging. But if you have other environments that you also want, need, etcetera, like a QA environment or a UAT environment, things like that, you can go ahead and sort of create custom environments as well. Now this gate will only pass when this is actually development. We'll actually go ahead and filter down sort of the rule sets to find which rules are applicable to the environment that you've initialized with and and only respond with the the corresponding values. In addition to doing this for gates, similarly for experiments, you can also start them in lower environments before actually starting them in production. The final thing is, like, how do you actually go and make sure that an existing gate, etcetera, is looking healthy? That's where our diagnostics come into play. In previous videos, we've talked a lot more about sort of the exposure stream down at the bottom. This is like your in the weeds. I'm getting in there. I'm understanding what individual exposures look like, what rule they're getting, things like that. But we also have a number of health checks that run on top of all of your things. Sort of the most straightforward one is just like our checks coming through. Worth noting it is sort of per rule for gates. For experiments, less concept of rules. It's one set of health checks, but very similar set of things. And we'll do things like say, hey. Are checks coming through in general in production? When you're doing checks, are you including the unit type that you're supposed to? In this case, for this demo, I included a lot of checks where I didn't have a user ID. But given that this is a gate that randomizes on user ID, I should probably be including one. So that's where this health check would come into play. And then there's just things like, you know, are we seeing the right sorts of assignment reasons? We'll do things like check for sample ratio mismatch. If you configured a fifty fifty split and we're seeing that it has a twenty five seventy five split, that probably means something is wrong somewhere, and that's where that health check would fire as well. These health checks are really there to be your friend in terms of making sure you can check and understand if a gate is healthy. For gates that or rules that should have metrics. So places where I have some, you know, fifty fifty split like this one. We actually can have a control and test group going. We'll start to do things like check those sort of more metric specific ones. Are the user IDs that we're seeing lining up with the user IDs you have data for for metrics? If they aren't, it maybe means that thing we've seen is that people hash their IDs in one place and not in another. And it just means that the join isn't gonna be able to happen, right, to, like, say, test static value of x and control had a value of y. So these health checks are there to sort of be your friend, help you be aware of things early, get ahead of them, and sort of not waste days and then later realize, oh, shoot. We had a problem and we have to restart everything. That's sort of how you go ahead and test out your code for yourself or for, you know, a select group of dog fooders, etcetera, how you start running it in lower environments, and then finally, how you go ahead and make sure everything's looking healthy once it's sort of live in production. Thanks.

---

## Statsig 3.3 - Analyze existing experiment data

_Source: `data/raw/captions/Statsig 3.3 - Analyze existing experiment data.txt`_

Once you have created all your metric, the next step is to add them to your experiment for analysis. In this chapter, we will do a quick demo of the experiment result page. On the result page, you will find two main sections. The first section is the cumulative exposure charts, which you will find how many users in different test groups has been exposed to your experiment over time. This will also be a good way to help you cross check if the exposure volume has meet your expectation. In the second section, which is also what we call the scorecard section, you will be able to find all the metrics you have chosen for your experiment. Each metric has a color coded bar. Either it's green for statistics significant in the positive direction, or red means it's statistics significant in the negative direction. When the bar is showing grey, it just means the metric is not statistically significant. Along the bar, you will also be able to see the mean lift percentage for the metric, as well as the confidence interval. If you hover or click on a metric, you can get even more details about the underlying statistic and all the other relevant information. Let's click on it and you will see a pop up window. Here is all the statistic statistics about this revenue metric that we are looking at. There is also a time series view tab for day to day view, where you can have a day to date view for the cumulative metric performance, as well as a day since first exposure chart for you to detect any novelty effect. Another tab is called variance reduction. It helps you to see how much variance in your metric has been reduced successfully by applying CUPID, which is our variance reduction techniques. Last but not least, on this pop up window, you will find the Impact tab. Under the Impact tab, you will not only see the absolute and relative experiment delta for your metric, but also the top line impact and the projected launch impact pre calculated for you. One more thing to highlight on the experiment page is our house checks. On the right hand side of the result page, you will be able to see called experiment quality score that evaluates how well the experiment is set up, and also a house icon, which shows how well your experiment is running after it has been started. When you hover on here, you have the option to click on view details. It will take you to our diagnostic tab where you can take a closer look at the experiment health checklist, which covers many critical important experiment health checks, including whether the exposures are balanced, whether there are cross over users, or whether there are differential impact detected. We also have a built in self serve troubleshooting tool for you to troubleshoot if any of the items in this list have not successfully passed the health check.

---

## Statsig 3.3 - Connect events

_Source: `data/raw/captions/Statsig 3.3 - Connect events.txt`_

Hi, I'm Tyler Van Herren, an engineer here at Statsig, and today we're gonna be talking about getting your events and metrics into Statsig if you're using Statsig's cloud. Today we're gonna be talking about getting your events and metrics into Statsig when you're using Statsig's cloud offering. And so there's broadly a few different ways to do this, and what'll make most sense for you will probably be a bit dependent on how much infrastructure you already have in place. The sort of most direct way to do this is going to be using Statsig SDKs, which will sort of come in two forms. The first is if you're using one of our JavaScript SDKs in particular, there's already gonna be some auto capture events that you can enable or you can have There's already gonna be some auto capture events that you'll get by default unless you choose to disable them. And so those will give you sort of a really good baseline of just click events and things along those lines that can just help you broadly understand your user behavior. And then where it gets really interesting are going to be the things that you want to log pretty directly. What that'll look like is just calling dot log event on the StatsSeq SDK that you're using, whether that is client or server side, and just providing what event happened, what values associated with it, and then any metadata you might have. And so this could look something like saying, hey, a purchase event happened. It was a purchase for five dollars, and by the way, it was for this product and this product category, and things like that that might be useful for drilling into later on. And so again, that sort of SDK approach is gonna be the sort of simplest if you don't already have a lot of this set up. That being said, many of our customers are coming to us with a lot of this in place. And that'll sort of come in two levels of maturity. The first one that I'll hit on is if you are already logging stuff to Statsig using something like a Mixpanel or oh, I messed up. Alright. Beyond the folks, for the folks who already have things set up, there's gonna be sort of two levels of maturity we see there. The first is going to be folks that are already logging a lot of their stuff via one of these CDP providers, so to speak, such as a Mixpanel or a rudder stack or a Segment. This is a really common setup we see people have, where they're already logging a bunch of stuff here so that they can send it along its merry way to all sorts of tools that they might be using, Statsig being one of them. And so using RudderStack as an example, what this is really going to look like is just setting Statsig as a destination for your events, and then you'll get to do some interesting filtering on top of those events to make sure that you're only sending the types of things that you actually care about our way. That's going to allow you to really quickly get up to speed with the type of stuff you already have logged and all the work you've already done on the telemetry side there across your application, whether that is front end or back end. For that, once you have that connection set up, whether that's filtering that you're doing on that end or filtering you're doing in the Statsig side of house, you're able to restrict which things you actually care about to make sure that you're not sending wasteful material. The other thing that'll be really common for folks is they might already have a lot of this stuff in their warehouses. And that can take sort of two forms. The first are these event level things that we've been talking about for sending stuff through the Statsig SDK or sending things through like a segment to our router stack where it really is just representing here is a thing that happened. And there's some metadata around it that you can slice and dice and you can do interesting aggregations on top of. In Statsig, what that looks like is creating a metric on top of those events. That again does those interesting aggregations, whether that's I wanna know how many unique people did this thing in a given day or over the course of an experiment, or I wanna know the sum of this value, creating a custom metric on top of that is gonna be the way to go. But you might also find that you already have a lot of information getting calculated in your warehouse that you want to ingest to us at a metric level. What those tables are going to look like is going to be those types of things where you might already have a table that has for a given day, for a given user, for a given metric, they have a value of x. And then you'll be able to ingest that pretty directly into Statsig and make use of that. Again, while many people that are in that state often find themselves choosing to go to Statsig warehouse native, there's also plenty of folks who find it pretty convenient to just be able to upload that stuff to Statsig directly and have us sync it over to us. What that's going to look like really is just adding an ingestion pipeline from your provider of choice, wherever you might have your data today. I'll just use BigQuery as an example. And helping us make sure that alright, I won't click that button. I'll close that. Using BigQuery as an example. And what that's going to look like is it'll set up just making sure that we have the right permissions, whether that's from a service account or otherwise, again, depending a little bit per integration, and then adding some simple mappings around which tables we should be pulling from and how we should think about some of those columns. Again, that's going to be you telling us, hey, for a metric, these are the columns that represent a user ID or device ID or whatever ID type you care about in that instance. Here's the value column, here's maybe the name of that metric, things like that. And then similarly for an event, you'll be saying, hey, I have this big table of things that happened. Here is the column that is what thing happened, and then here's the different informational columns about it, as well as when it happened has some timestamp, and also who it happened for. And so that's gonna be sort of the most effective way if you're already pretty mature from a data perspective to make sure that you can just bring all that stuff into Statsig and be able to use it in all of the different experiments that folks are running. We're huge fans of being able to use sort of the business metrics you already care about in the context of experiments. Thanks.

---

## Statsig 3.3 - Connect your warehouse

_Source: `data/raw/captions/Statsig 3.3 - Connect your warehouse.txt`_

Hi, my name is Xing Qi. I'm a customer data scientist at Stasic. In this section of the course, we're going to talk about how to connect your data to Stasic, and how to create a new metric source within Stasic Console. Once you're on the metric source interface, under the data tab, you'll just click on the create button to create a new metric source. For example here, we're going to name something like, My First Blogging Event. You can also add a short description to provide more information, and you can also create a tag for it for better discoverability. Sometimes it's also helpful to attach the metric source to your team, for better governance and to manage the review process for any potential future changes in your metric source. Go ahead and click on confirm, and then you will be linked into the metric source creation interface. You have two options to connect your data table to Stasic. Either you can directly provide a table name, which provides a one to one mapping to a table that sits in your data warehouse. Or, you have the option to provide a query. This gives you flexibility and room for customisation. When you do use the query option, we highly recommend for you to use static date macros to speed up your query and reduce the computation cost. Now I have inputted my query, you can go ahead and click on the Run Query button. This will give you a preview of the table you just created, and it's a good way to cross check if everything looks correct to you. In general, you can use many different types of table as input as your metric source. It might be a raw event logging table, or you can also use an aggregated table that is aggregating data at the granularity that your business cares about. We also see some customers, they will directly use an aggregated metric table at per user per day granularity, and then build their metric on top of that. Moving on to the bottom section of the metric source setup, there are two mandatory fields you have to configure. The first one is a timestamp value and the other one is a unit ID, both of which will be used to join to the exposure events table for experiment analysis. In this case, I'm just going to pick TS as my timestamp column, and user ID as my configured static unit ID. You have the option to check the box to treat timestamp as a date, if that aligns with what your data is providing. By checking this box, you are basically telling Static that your data columns use a date rather than an exact timestamp. With this, Static will use the date of the exposure events instead of the precise timestamp when performing the join to the experiment exposures. Then you will be all set to create your new metric source.

---

## Statsig 3.3 - Define and create your metric catalog

_Source: `data/raw/captions/Statsig 3.3 - Define and create your metric catalog.txt`_

Once you have created your metric source, the next step is to build your first metric. In the metric catalog service, click on the create button to create your new metric. In this example, we're going to call our first metric purchase revenue. You can also add a short description to provide more context about the metric you're creating, and also adding tags or attached to a team. But most importantly, you will need to select the metric source that you want to view this metric on top of. In this example, we are just going to use the metric source we just created from the last section of the course, my first login events. And then you can go ahead and click create. Once you land in the metric creation surface, to the left, you will see the option to choose your metric type. Under the drop down menu, Static offers a very wide range of different metric types to support different business use cases. For example, we have count metric, sum metric, unit count, which is essentially a We also have count distinct mean, first value, latest value, ratio metrics, and some other more advanced metric type like retention metric, funnel metric, and percentile metric. In this quick example, I'm just gonna create a simple sum metric to get you guys started. Under the source section, you will see the metric source which is selected popping up. But you also have the option to add multiple different sources. And Static will just do a union across different metric sources and conduct the aggregation over everything. In example, we will just stick with the one metric source that we have just set up. Under here, value column to sum, we will pick price USD as the value column we want to aggregate over. You also have the option to add filters to your metric source by using these icons. It can be directly using some buttons within the console, or you have the option to add SQL filters if you want. Now this will be a basic setup for your sum metric. Now go ahead and click on run query. On the right hand side, we will give you a few samples about how the data looks like from the metric source you have configured and also a preview of the metric value over time in the last thirty days. If everything looks correct to you, then we can moving on to the more advanced section of the metric configuration. The first one is the metric breakdowns. This is where you can set up the dimension that you'd like to analyze your metric results by. For example, in this case, we can pick product categories as the as a breakdown. Then whenever the scorecard are loaded or refreshed, Static will automatically precompute that segment data so you can view it directly in your scorecard. Under experiment settings, there are several additional options. You can do outlier management with either windsorization approach, or you can set a metric cap to capping the outliers. There are also several additional options. You can enable QPID, which is a variance reduction technique that helps you reach statistical significant results in your metric faster. By default, we will configure seven day look back window, but you can feel free to change it based on your own business context. Another option is to measure your sum metric as a threshold. This allows you to see what percentage of the user in your experiment has reached a certain threshold you have configured. You also have the option to apply a corporate window, which is useful for metrics like revenue. You might want to apply a measurement window, for example, like seven days revenue after the user's exposures. We also support baking windows. These are especially helpful for metrics that can take time to fully stabilize. For example, if you have a refund metric, the data might require one or two weeks to come in, so applying a baking window ensures you are looking at the results only after they are matured. Now this is just a a overview of the setup of a sum metric. There are also a lot of other different metric types, and different metric types may have slightly different advanced settings for different common use cases. But we usually will include these two tips throughout the interface to explain what each options does. And if you do want to dive deeper, feel free to click on the learn more button, which will link you to a detailed documentation for each feature we support. Now we can submit the metric for review, and your first metric is successfully created.

---

## Statsig 3.3 - Test - analyze feature gates and experiments

_Source: `data/raw/captions/Statsig 3.3 - Test - analyze feature gates and experiments.txt`_

Hi. I'm Tyler Van Herren, an engineer at Statsig, and today we're gonna be talking about how to actually go ahead and understand if things are looking good once you have an experimenter gate set up in code and you're sending some events our way. So the first surface that I wanna talk about is going to be diagnostics. This is sort of your place to go understand if the experimenter feature gate itself is healthy. If we're seeing the things we expect to see from that perspective. And then results are gonna be where we can go and actually talk through, is this good for my product? Is this good for my user base? Is this good for the metrics I care about? And so there's a few different things I wanna call diagnostics surface. The first one that you'll be met with are a bunch of health checks. For experiments, you'll just have one big health check section. And then for a gate, you'll actually have several different options to choose between the different rules that you might have configured on it. But what this is going to do is give you a number of diagnostics that can help you understand just like, again, high level, are things looking good? And so this includes things like just are we seeing checks in general, of course. If I said that I was going randomize off of user ID or even a custom ID like company ID or things like that, are we providing that consistently in our user object that we're checking this thing with? And then sort of the other example there being, I said this was gonna be a fiftyfifty test or a fiftyfifty split on the feature gate. How close am I to that? Am I sort of within bounds there? And for all of these, in this case they're all passing, but when we have things that we're gonna flag, you'll be able to get some little next steps of what to dive into to take a look at them. Again, for some things, it might be a little more self explanatory than others. For instance, if you're not providing the user ID, it probably does just look like providing the user ID, making sure you're doing that when you're initializing. But then for others, like sample ratio mismatch, this exposure is our balance check, that's where there's probably a little bit more nuance of understanding is this specific to a given platform, or things like that to understand where that mismatch might be coming from. And so, in addition to these health checks that again have some cool debugging tools in them, there's also one of my favorite surfaces, the exposure stream. This is going to be a pretty live and pretty raw list of the types of people, or the people I suppose, that are checking your gates. Again, from whatever surface they might be coming from. And you can actually click on any of these entries and see exactly which user it was that checked this, again with things like private attributes filtered out, and see why it got that evaluation. In particular, this reason bucket is pretty nice for understanding, am I checking this before I'm initializing or something that. Or if you're falling back to cache pretty consistently, for instance, if your user's network conditions are not so good. For server SDKs as well, you're able to see when things synced over and things like that to really understand why they actually got that result and rule, again, for that user object. This is also something where you're able to toggle it over to non production logs for those sort of more diagnostic type things while you're trying things out locally yourself. Worth noting that you'll already be toggled over here if you don't have any production logs. But you're also able to filter this in a number of different ways, whether that's different days you wanna look at, or if there's different attributes you want to zero in on in particular, whether that's the rule, the response, the reason rather, or things about the user. So I wanna see my iOS users and at a glance understand, oh, they are getting the assignments I expected, I can do that sort of thing via those filters. The final thing I'll call out on the diagnostics page is going to be the checks chart. This is just a really good option for understanding broadly how much traffic is this getting, and again, something that you can look at per rule for gates which have those rules, or sort of overall for experiments, and just understand those traffic patterns of when are people checking this thing. And so those tools, sort of in combo, can give you a pretty good sense of are things implemented the way you expect from the stat sync perspective? On the other hand, the sort of other interesting thing to take a look at here is of course going to be what is the actual impact that I'm having on the metrics I care about? And that's where the results page or the metric impact page in the case of gates is going to come into play. For our cloud users, this is going to be something that generally gets refreshed once a day. There's a few things that'll come in a little bit live or to give you sort of a quick early look, again especially early on in an experiment, but mostly it's going to be calculated once a day. And for our warehouse native users, it's going to be calculated sort of when you tell us to calculate it, which often does also result in being once a day, sort of based on these schedules we often see people set. And this is going to give you, first off, just that broad picture of how many unique users or units, as the case may be, because these are not necessarily user level tests. How many unique units I've been exposed to this thing over time? Does this match the sort of ramp up strategy I had imagined? Does this match my sort of understanding of what percentage of my users on a given day are net new versus returning relative to some sort of earlier start time? As well as the actual metrics themselves, of course. For an experiment, this will be broken up into your primary and secondary metrics, and for a feature gate, this is going to be sort of your big bucket of monitoring metrics. And for all of these, again looking at a frequentist view here I suppose, you'll be able to see is there a statistically significant movement, aka is that bar going to be red or green, or is there something that's not stat sigma, aka these gray bars. And these are going to give you an understanding of how your test group or groups are performing relative to your baseline control group. For feature gates, that's just going to be your false users as the baseline and your true users as the sort of test group there. And then for experiments, you're a little bit more explicit about which group is the control, usually the first one. And so yeah, this is gonna be where you can go to just broadly understand is there movement, what kind of movement. The really quick summary as well is that for the frequentist view like I'm looking at here, which is also our default, the wider the bar, the more uncertainty you should attribute to those results. And then the sort of black bar in the middle is going to be where that actual mean movement is going to be if you were to take a look at it. The other thing I'll call out here is that for most metric types, this is something that you should think of as already being normalized per user. That's a sort of common misconception we see folks have, but for instance, if you have a ninety nine percent group split to a one percent split, you would expect the ninety nine percent group to have ninety nine times the raw values, so it makes sense to normalize per user. And we're going to be doing that for most metric types, sort of except for some of those ratio type calculations. So this is giving you an idea of for a unit in test versus a unit in control, how much higher should I expect these metrics or lower should I expect these metrics to be? And so again, that's sort of your window back into the world of all the different metrics you care about from a product perspective, whether that's performance metrics like page load time, spend per user, anything that you might care about, as long as you can quantify it at that per unit level, you'll be able to understand what in fact your changes are having on those types of metrics. Thanks.

---
